namespace messages;

enum FireStatus:byte { ACTIVE = 0, CONTAINED = 1, EXTINGUISHED = 2 }

table FireMessage {
  timestamp:long;
  id:uint;
  latitude:float;
  longitude:float;
  status:FireStatus = ACTIVE;
}

root_type FireMessage;